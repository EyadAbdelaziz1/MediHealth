version: '3.8'

services:
  server:
    build:
      context: .
      dockerfile: Dockerfile.server
    ports:
      - "3000:3000"
    env_file:
      - server/.env
    environment:
      - NODE_ENV=production
      - CORS_ORIGIN=${CORS_ORIGIN:-*}
    restart: unless-stopped
